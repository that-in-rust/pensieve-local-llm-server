// Sample Rust code file
fn main() {
    println!("Hello, world!");
    
    let numbers = vec![1, 2, 3, 4, 5];
    for num in numbers {
        println!("Number: {}", num);
    }
}

// This is a comment
struct Person {
    name: String,
    age: u32,
}