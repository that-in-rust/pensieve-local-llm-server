To ensure all Jupyter Notebook cells have word wrapping enabled by default, you can modify the Jupyter Notebook's custom CSS file. Here's how you can do it:

1. Locate the Jupyter Configuration Directory:
   - Run the following command in your terminal to find the Jupyter configuration directory:
     jupyter --config-dir

2. Create a Custom CSS File:
   - Navigate to the custom directory inside the configuration directory. If it doesn't exist, create it.
   - Inside the custom directory, create a file named custom.css.

3. Add CSS for Word Wrapping:
   - Open custom.css and add the

